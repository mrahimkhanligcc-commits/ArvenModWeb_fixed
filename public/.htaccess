RewriteEngine On

# Do not rewrite existing files or directories.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Send every other request to the front controller.
RewriteRule ^ index.php [QSA,L]
